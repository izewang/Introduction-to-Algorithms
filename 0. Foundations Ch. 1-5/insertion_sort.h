#ifndef insertion_sort_h
#define insertion_sort_h

#include <vector>

// In place sorts nums by insertion sort algorithm. Worst case takes O(n^2) computing time.
void InsertionSort(std::vector<int> & nums);

#endif
