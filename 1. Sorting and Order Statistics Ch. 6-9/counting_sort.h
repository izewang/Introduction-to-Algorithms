#ifndef counting_sort_h
#define counting_sort_h

#include <vector>

// Sort nums range in [0, n] in linear time by counting sort algorithm.
void CountingSort(std::vector<int> & nums, int maximum);

#endif
