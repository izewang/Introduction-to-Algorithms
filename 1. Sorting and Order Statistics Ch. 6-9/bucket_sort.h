#ifndef bucket_sort_h
#define bucket_sort_h

#include <vector>

// Sort numbers of [0, 1) in linear time.
void BucketSort(std::vector<double> & nums);
// Sort numbers worst in O(n^2) time.
void InsertionSort(std::vector<double> & nums);

#endif
