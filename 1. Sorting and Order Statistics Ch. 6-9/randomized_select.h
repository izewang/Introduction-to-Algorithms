#ifndef randomized_select_h
#define randomized_select_h

#include <vector>

// Randomly selects kth number in order from nums[p] to nums[r].
int RandomizedSelect(std::vector<int> & nums, int p, int r, int k);

#endif